<html>
	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
	
<link rel="stylesheet" href="/index/style.min.css">
	<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
	<script src="https://kit.fontawesome.com/8dd91d71f4.js" crossorigin="anonymous"></script>

<script src="/index/script.min.js"></script>
<script>
const div = document.createElement('div')
const title = document.createElement('h1');
const p = document.createElement('p');
;(async () => {
const info = await fetch('/api/user').then(r => r.json());
console.log(info);
title.innerHTML = info.name;
if(info.owner) title.innerHTML += " (GOD)";
if(info.createdAt) p.innerHTML += `Created At ${new Date(info.createdAt).toLocaleString("en-US", {timeZone: timezone })}`;

div.append(title);
div.append(document.createElement('br'));
div.append(p)
document.body.append(div)
})()
</script>
<div class="post-box">
  <textarea rows = "4" cols = "40" name = "description" id="post-input"></textarea>
<div class="button-box">
	<span></span>
      <span></span>
      <span></span>
      <span></span>
<button id="post-btn">Post</button>
</div>
</div>

<div class="posts" id="posts"></div>
</html>